# dependencies
node_modules/

# Next.js build output
.next/
out/

# environment variables
.env
.env.local
.env.local.backup
.env*.local
.env.*.local

# TypeScript
tsconfig.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
.vercel
.env*.local

# === Bug #24 cleanup (17 mai 2026) ===

# Agent tools artifacts (impeccable + Claude Code skill installations)
.agents/
.claude/skills/impeccable

# Design system cache (regenerable via `impeccable document`)
.impeccable/

# Playwright MCP test artifacts (session logs + snapshots)
.playwright-mcp/
landing-prod-snapshot.md

# Playwright auth storageState — contains a live Supabase session token
scripts/.playwright-auth.json

# Env file backups — CRITICAL (gap closure : .env*.local pattern actuel ne couvre PAS .backup.*)
.env*.backup*
.env.local.backup*

# Dev screenshots in repo root (assets app dans /public préservés)
/*.png

# A1 provider test scripts (throwaway, never commit)
scripts/a1-test/

# Claude Code local settings + backups (personnel, peut porter des tokens)
.claude/settings.local.json*
